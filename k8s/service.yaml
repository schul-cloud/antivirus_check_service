---
apiVersion: v1
kind: Service
metadata:
  name: $SERVICE_NAME_SCANFILE
  labels:
    app: $SERVICE_NAME_SCANFILE
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 3030
  selector:
    app: $SERVICE_NAME_SCANFILE
    tier: web
